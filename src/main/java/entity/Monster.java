package entity;

public class Monster extends Creature {

    public Monster(int attack, int defense, int maxHealth, int minDamage, int maxDamage) {
        super(attack, defense, maxHealth, minDamage, maxDamage);
    }
}
